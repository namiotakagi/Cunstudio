@model IEnumerable<Cunstudio.Models.HopDong>

@{
    ViewBag.Title = "Danh sách hợp đồng";
    Layout = "~/Views/Layout/AdminLayout.cshtml";
    WebGrid grid = new WebGrid(source: Model, rowsPerPage: 15);
}
<main>
    <div class="row justify-content-center">
        <div class="col-lg-7">
            <div class="card shadow-lg border-0 rounded-lg mt-5">
                <div class="card-header"><h3 class="text-center font-weight-light my-4">Danh sách gói dịch vụ</h3></div>
                <div class="card-body">
                    <input class="btn btn-primary btn-block" onclick="location.href='@Url.Action("TaoMoiHD","QuanLyHD")'" value="Tạo mới" />
                    @grid.GetHtml(
                             tableStyle: "gridTable",
                             headerStyle: "gridHead",
                             footerStyle: "gridFooter",
                             rowStyle: "gridRow",
                             alternatingRowStyle: "gridAltRow",
                             columns: grid.Columns(
                             grid.Column("ID_HopDong", "Mã hợp đồng"),
                             grid.Column("ID_KhachHang", "Mã khách hàng"),
                             grid.Column("ID_Admin", "Mã admin"),
                             grid.Column("ID_Goi", "Mã gói dịch vụ"),
                             grid.Column(format: p => Html.ActionLink("Chỉnh Sửa", "ChinhSuaHD", new { id = p.Id_Goi })),
                             grid.Column(format: p => Html.ActionLink("Xoá", "XoaHD", new { id = p.Id_Goi }))
                             ))
                </div>
            </div>
        </div>
    </div>
</main>





<style>
    .gridTable {
        border: 1px solid #808080;
        width: 100%;
    }

    .gridHead {
        border: 1px solid #808080;
        background-color: lightblue;
    }

    .gridRow {
        border: 1px solid #808080;
    }

    .gridAltRow {
        border: 1px solid #808080;
    }


    .gridFooter {
        font-weight: bold;
        border: 1px solid #808080;
    }
</style>